# These commands will be used to setup demos and pull down scripts from GitHub - Copy/Paste the below commands into Cloud Shell PowerShell Environment

curl https://raw.githubusercontent.com/amanouk/content-az700-Microsoft-Azure-Network-Engineer-Associate-main/master/21-Monitor-Networks/21-02-traffic-analytics-demo-deploy.json -o setup.json

# Create a Resource Group 
az group create --name brandrg2 --location westus

#You can then run:
$rg = "brandrg2"

az deployment group create --resource-group $rg --template-file ./setup.json
